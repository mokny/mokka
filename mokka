#!/usr/bin/env bash
ORIGINFILE=${readlink -f /usr/bin/mokka}
ORIGINDIR=${ORIGINFILE%/*}/
cd "$(dirname "$0")"
cd "$ORIGINDIR"

if command -v python3.12 &> /dev/null
then
    python3.12 mokkas "$@"
else
    python mokkas "$@"
fi
